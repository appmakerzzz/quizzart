#include<stdio.h>
int main()
  {  unsigned long long int n,i,b=0,c=1,d,number,MAXI=10000000000,a[50];
      for(i=0;b<MAXI;i++)
         {   a[i]=b;
              d=b+c;
              b=c;
               c=d;
              if(b>MAXI)
                break;
               count++;
              
          }
         

       
     scanf("%llu",&n);
      while(n--)
       {  
          scanf("%llu",&number);
          for(i=0;number>a[i];i++)
               ;
           if(a[i]==number)
             printf("IsFibo\n");
            else
             {
          printf("IsNotFibo\n");
        }}
  return 0;
}
                 
             
